# E00 accepted implementation report — PSYCHE OS

**Date:** 2026-08-11
**Epic:** E00 — Minimal Irreversible Secure Core
**Status:** `ACCEPTED`
**Decision boundary:** ADR-021
**Data profile:** bundled synthetic fixtures only
**REAL_DATA_GATE:** `CLOSED`

## Accepted result

E00 now supplies the minimum irreversible foundation for later synthetic-only
epics: canonical version/history semantics, typed temporal/provenance/policy and
deletion behavior, fail-closed encrypted storage profile evidence, minimal
transactional migrations, package-owned synthetic fixture writes, open logical
export and truthful scoped validation.

The final independent acceptance is
`docs/implementation/E00_ACCEPTANCE_REPORT.md`. Its focused production-path
review closed F05 migrations, F07 synthetic isolation, F08 validation and the
deferred-surface lock.

## Final gate

| Command | Result |
|---|---|
| `uv run pytest -q tests/unit/test_versions.py tests/integration/test_storage_integration.py tests/integration/test_e00_rebaseline_gate.py` | `48 passed` |
| `uv run python scripts/validate_f0_scope.py` | `PASS` |
| `uv run python scripts/validate_f0_artifacts.py` | `PASS` |
| `python scripts/dev/validate_orchestration.py` | `118 passed, 0 failed` |

## Acceptance-time local corrections

- Replaced migration-time `executescript()` with complete single-statement
  execution inside one transaction; froze the V1 checksum and added rollback
  regressions for implicit-commit and clean-database failure paths.
- Applied the shipped Draft 2020-12 export schema through `jsonschema`, added
  structural SQLCipher evidence checks, and kept deferred capabilities visibly
  `DEFERRED_NOT_READY`.
- Strengthened fixture manifest/schema/table/digest validation and authority
  shape validation.
- Locked backup/restore, blob writes and filesystem mutations before side
  effects with `FEATURE_DEFERRED_PRE_REAL_DATA`.

## Deferred profile work

- F02 backup/restore: E01, High, disabled.
- F05 blob lifecycle: E01, High, disabled.
- F09 Windows filesystem hardening: E01, High, runtime not verified, disabled.
- F08 release-grade supply-chain evidence: E11, unsatisfied.

E01 must activate a deferred subsystem only when its bounded assurance work
actually requires it. No E00 result changes `REAL_DATA_GATE` from `CLOSED`.
