# Epic E11 — implementation report

**Status:** `IMPLEMENTED`
**Commit:** `uncommitted`
**Risk:** `RISK-H`

## Implemented

- Added strict JSON Schemas and deterministic, side-effect-free evaluation for
  one exact candidate's profile, source/build/platform, lock/SBOM/artifact,
  raw-evidence, review, expiry, residual-risk, seal, successor, and human
  attestation bindings.
- A DRAFT never supports `OPEN`; a SEALED payload must match its digest; changed
  invalidated facts require a distinct successor; missing/expired/mismatched
  evidence, reviews, reproduction, required review classes, exclusions, or
  human attestation all return `CLOSED`.
- Added one repository-owned synthetic DRAFT release manifest/evaluation for
  the canonical pre-E11 source. It truthfully remains incomplete and `CLOSED`.
  No human attestation or authorized `OPEN` decision exists.
- Repaired two stale gate-state tests and the research validator to use the
  accepted stable-policy/live-state split and to exclude ignored/generated
  worktrees/dependencies while retaining nonignored workspace files in hygiene
  scans.

## Key files

- `src/psyche_os/release_evidence/e11_gate.py`
- `schemas/e11/gate_evaluation.schema.json`
- `schemas/e11/human_attestation.schema.json`
- `scripts/validate_e11_gate_evaluations.py`
- `artifacts/e11/gate-evaluations/e11-implementation-candidate-draft.yaml`
- `artifacts/e11/release-manifests/e11-implementation-candidate.yaml`
- `tests/unit/test_e11_gate_evaluation.py`

## Migrations / data impact

`none`. New artifacts contain only repository-owned synthetic metadata. No
canonical store, product schema, stable gate policy, profile definition, or
real-data state was modified.

## Validation

- Targeted E11 evaluator tests: `17 passed`.
- Targeted stale gate-state regression tests: `12 passed`.
- E11 read-only validator: `1 closed evaluation record inspected, 0 failures`.
- FULL `uv run pytest -q`: `817 passed, 2 skipped` (Windows symlink creation
  unavailable without required privilege in the pre-existing E10 and regression
  cases).
- `uv run python scripts/dev/validate_orchestration.py`: `131 passed, 0 failed`.
- `uv run python scripts/validate_research_foundation.py`: `0 errors, 25
  warnings`; warnings are existing source-date and duplicate-heading findings.
- `git diff --check`: passed.

## Constitutional / security notes

`REAL_DATA_GATE` remains `CLOSED`. The evaluator does not modify application
state, manufacture evidence, seal a file in place, or create a human decision.
Coding-model and wrong-digest attestation paths are tested as closed failures.

## Deviations

`none`.

## Known limitations

- The DRAFT candidate has no reproduction evidence, current qualified reviews,
  or `REPOSITORY_OWNER` human attestation. It is intentionally not release-ready
  and remains closed.
- The future local human-attestation format records a declared owner decision;
  E11 adds no PKI or independent identity-verification service.

## Next dependency

No successor epic is defined. A future authorized owner may create a new exact
candidate evaluation and separately provide a current
`local_human_attestation_v1` record; it must not alter this DRAFT record.
