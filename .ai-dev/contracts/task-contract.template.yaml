# Compact Task Contract for ordinary LOW/MEDIUM product work.
# The user task itself may be this contract. It pre-authorizes only listed actions.

GOAL: <outcome>
SCOPE: <allowed paths and behavior>
DO NOT CROSS: <boundaries, dependencies, migrations, permissions, real-data, secrets>
ACCEPTANCE: <observable functional result>
AUTONOMY + VERIFY: <autonomous repairs; targeted checks; one final gate or exact-SHA PR CI>
OUTPUT: <commit, draft PR, residual/deferred item if any>

# Add a short explicit approval only for destructive/irreversible actions, production
# dependencies, migrations, permission/network expansion, crypto/key changes,
# REAL_DATA_GATE changes, secrets/accounts/billing, or material security architecture.
